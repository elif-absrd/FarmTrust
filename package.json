{
  "name": "farmtrust-workspace",
  "private": true,
  "version": "1.0.0",
  "main": "./app/node_modules/expo-router/entry",
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "pnpm --dir app start",
    "app": "pnpm --dir app start",
    "backend": "pnpm --dir backend run dev",
    "prisma:generate": "pnpm --dir backend run prisma:generate",
    "prisma:push": "pnpm --dir backend run prisma:push"
  }
}